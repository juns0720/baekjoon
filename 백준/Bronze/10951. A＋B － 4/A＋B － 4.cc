#include <stdio.h>

int main()
{
    int n, x;
    int answer;
    

    while (scanf("%d %d", &n, &x)!=EOF)
    {
       
            printf("%d\n", n + x);
    }


 

    return 0;
}