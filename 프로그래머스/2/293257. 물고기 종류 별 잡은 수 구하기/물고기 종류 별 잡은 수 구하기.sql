







SELECT 
    COUNT(*) AS FISH_COUNT,
    FISH_NAME
FROM (SELECT ID, FISH_NAME, A.FISH_TYPE, LENGTH, TIME
FROM FISH_INFO A
JOIN FISH_NAME_INFO B
ON A.FISH_TYPE = B.FISH_TYPE) AS A
GROUP BY FISH_NAME
ORDER BY FISH_COUNT DESC
    